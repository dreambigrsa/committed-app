/** Feed bootstrap — aligned with `contexts/AppContext.tsx` loadUserData. */
export const APP_FEED_POSTS_LIMIT = 50;
export const APP_FEED_REELS_LIMIT = 50;

/** Embedded user row on posts/reels — same fields as AppContext `postUserSelect`. */
export const APP_POST_USER_SELECT = 'full_name, username, email, profile_picture';
