/**
 * Stored on `user_onboarding_data.onboarding_version` — must match everywhere
 * we upsert onboarding / Committed AI consent (Expo + Next).
 */
export const COMMITTED_AI_ONBOARDING_VERSION = '1.0.0';
